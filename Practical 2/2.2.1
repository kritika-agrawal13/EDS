# Read the array
arr = list(map(int, input().strip().split()))

# Read the key to search
key = int(input().strip())

# Linear search
found = False
for i in range(len(arr)):
    if arr[i] == key:
        print(i)
        found = True
        break

if not found:
    print("Not found")
